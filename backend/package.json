{
  "scripts": {
    "start": "nodemon --ignore dist -e ts --verbose --exec \"yarn compile\"",
    "compile": "tsc && node ."
  },
  "dependencies": {
    "koa": "^2.13.1",
    "koa-router": "^10.0.0",
    "nodemon": "^2.0.7",
    "typescript": "^4.3.2"
  },
  "devDependencies": {
    "@types/koa": "^2.13.3",
    "@types/koa-router": "^7.4.2",
    "@types/node": "^15.6.1"
  }
}